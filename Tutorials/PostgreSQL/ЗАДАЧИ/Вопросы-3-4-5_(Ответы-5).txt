Урок 5
1. select 
	concat(first_name, ' ', last_name) as "Full Name"
   from 
	actor a;

  Все строки которые мы пишем в коде программы заключаем в ОДИНАРНЫЕ КАВЫЧКИ ''
 
  select 
	first_name || ' ' || last_name as "Full Name"
  from 
	actor a;

  ||  - операток конкотинации строк

2. select char_length('String 1')  -- 8
   char_lenght - возвращает длину строки
   select char_length('string 1' || ' ' || 'string 2') -- 17
   
Пример:
	select 
		char_length(first_name || ' ' || last_name)   as "Длинна строки"
        from
		actor a;

3.
	trim();    функция удаления всех пробелов вначале и вконце строки.
        ltrim();   функция удаления всех пробелов только в начале строки.
        rtrim();   функция удаления всех пробелов только в конце строки.

4.      substring('Andrii',1,3) -- And
        substring()      Функции выводит подстроку из задонной строки если в запросе указанна под строка длина которой привыщает 
                         строку то будет выведена то количество символов в подстроке которое есть в строке из которой берётся под строка
        substring('Andrii',1,3)  это выражение означает что верни мне подстроку из строки 'Andrii' начиная с первого символа 
                            !!!   И ВОЗЬМИ ТАКИХ СИМВОЛОВ 3 !!!!

5.      upper()     функция которая преоброзуют всю строку в верхний регистр
	select substring(upper(substring('andrii',1,1)) || substring('andrii',2,char_length('andrii')),1,char_length('andrii'))
               воводит имя с большой буквы
6.      lower()     функция которя преоброзует всю троку в нежний регистр

7.       Как удалить из строки заданную строку которая находится в начале строки или в конце?
         trim('er' from first_name)   удалит сооветсвие если оно находится в начале или в конце строки
         trim(first_name,'er')        также можно преведущий пример записать так результат будет такойже

8.       strpos('my_new_amail@.ks, '@')     если данное совподение найдено то возращает позицию первого совподение в строки
                                            !!! ОТСЧЕТ ИДЁТ С 1 !!!
         !!! ВОЗРАЩАЕТ 0  если совподение не найденно
	
   









