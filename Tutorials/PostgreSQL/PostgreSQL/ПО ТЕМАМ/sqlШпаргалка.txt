char_length()           функция которая возвращает длину строки
concat()                функция которая объеденяет строки
round(flot,0)           функция которая округляет число до целого
as                      функция alias
as ""                   позволяет вводить alias через пробел и вывод сохраняет регистр
trim                    уберает пробелы вначале строки и с конца строки
ltrim()                 убeрает пробелы вначале
rtrim()                 уберает пробелы вконце
substring(first_name,1,3) получени подстроки
lower()                   приведение строки к нижнему регистру
upper()                   приведение строки к вверхнему регистру
trim('er' from first_name) удалени подстроки
strpos(email, '@')        возвращает число позиции данного элемента
'%'                       ноль или неоределённое количество символов
'_'                       один любой символ 
(select * from film where rental_rate = 4.99)

WHERE:
(=,>,<,!=,<>,>=,<=)        сравнения значений употребляетс с ключивым словом where
between '2005-05-26' and '2005-05-28'  провереят входит ли значение в данный диапозон 
between 1 and 3                        употребляется с ключиевым словом where
like   'К_т'              сравнивает шаблон со строкой где _ обозначает любой символ
like   'A%'               указывает что соответсвие будет истиным есле строка начинается с А
like   '%W'               соответвие будет истиным есле строка заканчивается на W
not like '%Epic%'         соотвествие истино если в строке не подстроки Epic
is                        проверяет соответствие null и т.д.
is null
is not null  
in (2,3,5,6)              соответствие происходит по несколько значений
--                        однострочный комментарий
where ... and ...         
where ... or  ... 
-----------------------------------------------------------------------------------------------
    
Сортировка:

select *                               desc - сортирует по убыванию
from actor                             порядок команд select,from,where,order by
where first_name like '%a%'
order by first_name desc, last_name;   сортировать мохно не только по полям но и по выражениям

order by mod(actor_id,10)              сортировка происходит по выражению












