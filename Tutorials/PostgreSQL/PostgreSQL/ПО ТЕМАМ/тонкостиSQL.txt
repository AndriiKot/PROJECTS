substring(first_name,1,3) || ' ' || substring(last_name, 1,3)
    Pen Gui
    Ed Cha   результат сработал коректно(если под строка не может получить задонное количество символов
             то она возращает то значение символов которое есть).
order by (length / rental_duration) desc  если сортировка идёт по выражению то для проверки правельности запроса лучше 
					  в поле select указывать это выражение.

 