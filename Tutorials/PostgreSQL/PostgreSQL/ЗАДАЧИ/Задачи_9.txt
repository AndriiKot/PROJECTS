

1. Получите список название всех фильмов(film.title), рекомендованных для просмотра самыми маленькими зрителями.
   Для этого нужно объеденить результаты выпольнения двух запросов:
   1. получить все фильмы (film) с рейтингом G (film.rating = 'G')                              
   2. получить все фильмы (film), в которых снимался актер с фамилией Grant (actor.last_name = 'Grant'),
      поскольку этот актер по умолчинию снимается только в фильмах для саммых маленьких вне зависимости от проставленного рейтинга.
      Если какой-то фильм попадёт в оба запроса, то его нужно вывести дважды. Решить задачу с использованием union/union all/interset

2. Получить список названий всех фильмов (film.title) c рейтингом G (film.rating = 'G'), в которых
   снимался актер c фамилией Grant (actor.last_name = 'Grant'). Решить задачу с использованием union/union all/interset

3. Получить список названий всех фильмов (film.title) с рейтингом не 'G' (film.rating <> 'G'), в
   которых снималсф актер с фамилий Grant (actro.last_name = 'Grant'). Решить задачу с использованием union/union all/except/interset

