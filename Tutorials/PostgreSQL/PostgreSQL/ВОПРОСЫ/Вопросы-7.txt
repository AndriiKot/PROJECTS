1. Какое ключевое слово используетя для сортировки полей?

2. Как отсортировать из таблици "actor" все поля  в алфавитном порядке по имени то есть по полю "first_name".

3. Отсортируйте из таблици "actor" все поля в алфаитном порядку по имени и фамилии то есть по полю "first_name" и "last_name".

4. Если в запросе нам нужно задать огроничение и сортировку в каком порядке нужно выполнять эти команды?

5. Отсортируйте все aктеров по имени и фамилии в алфавитном по рядке у который в фамилии есть буква "a".

6. Расположите команды по порядку: from,order by,select,where.

7. Какое ключевое слово используется для сортировки по убыванию?

8. Какая сотрировка используется по умолчанию?

9. Отсортируйте из таблици "actor" все поля "first_name" по убыванию, а затем если есть актёры с одинаковым именем
   то поля "last_name" отсортировать по возрастанию.

10. Отсортируйте из таблици "actor" все поля "first_name" и "last_name" по возрастанию, 
    затем по убыванию 

11. Можно ли сортировать по выражению?

12. Отсоротируйте из таблици "actor" по последней цифры поля "actor_id".

13. Отсортируйте из таблици "actor" по псевдониму "first_name","last_name","actor_id" по возрастанию.
    затем отсортировать по убыванию.
    Скомбенируйте преведущий запрос с псевдонимом и полем.

14. Перечислите все способы по какому можно обратится к полю в сортировеу.

15. Какие есть ограничения при обрашение в сортировке если обрашаеся по псевдониму и по полю.

16. Какое ключевое слово используется для порядка сортировки?

17. Значения NULL при сартировке по умолчанию в каком месте будут насходтся в результирующем полу?

18. Если сортировка "order by" по умолчанию какая команда выведет  все значения NULL в начало списка?
    а также какая команда выведет все значения NULL в конце если сортировка указана по убыванию?
   Задача отсортируйте из таблицы "address" по возрастанию по колонуке "address2" так что-бы значения NULL
   были в начале списка.
   Задача 2 отсортируйте из таблицы "address"  по убыванию по колонку "address2" так что-бы значени NULL
   были в конце списка.




