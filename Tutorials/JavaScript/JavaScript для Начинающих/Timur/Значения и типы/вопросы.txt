
1. Раскажите про классификацию типов данных?

2. Раскажити про преметивные типы данных?

3. К какому типу относится значение "NaN" и "Infinity"?

4. Раскажите про тип данных 'BigInt'.

5. Раскажите про тип данных 'Number'.

6. Как в JS роспознать тип данных 'String'?

7. Раскажите про тип данных 'Boolean'.

8. let amount;
   Какое значение будет у переменной 'amount'

9. Какой тип данных возвращает функции в JS по умолчанию?

10. При неизвестном значении какой тип даных надо использовать?

11. Назовите все типы данных в данном коде?

const values = {
  name: 'Mouse',
  price: 25, 
  stock: false,
  avg: (a, b) => (a + b) / 2n,
  reference: null,
  primitive: undefined,
  numbers:   [NaN, Infinity],
};

for (const key in values) {
  console.log(typeof values[key]);
}


  

